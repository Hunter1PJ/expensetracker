package com.example.presentation.transactions

/**
 * Filter options for transaction types in presentation layer.
 */
enum class TransactionTypeFilter {
    ALL,
    EXPENSE,
    INCOME,
    TRANSFER
}
